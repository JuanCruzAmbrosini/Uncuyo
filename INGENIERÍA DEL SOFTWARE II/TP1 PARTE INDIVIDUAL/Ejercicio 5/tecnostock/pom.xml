<?xml version="1.0" encoding="UTF-8"?>
<!--
    POM (Project Object Model) de Maven.
    Define las dependencias de la arquitectura MVC + ORM + Seguridad + Vista solicitadas:

      - spring-boot-starter-web          -> Controladores REST/MVC, servidor Tomcat embebido
      - spring-boot-starter-thymeleaf    -> Motor de plantillas para la VISTA
      - spring-boot-starter-data-jpa     -> ORM (Hibernate) sobre JPA para el MODELO/persistencia
      - mysql-connector-j                -> Driver JDBC para MySQL
      - spring-boot-starter-security     -> Login por usuario y contraseña
      - thymeleaf-extras-springsecurity6 -> Integra Spring Security con las vistas Thymeleaf (sec:authorize, etc.)
      - spring-boot-starter-validation   -> Validaciones @NotNull, @NotBlank, etc. sobre los DTO
      - lombok                           -> Genera getters/setters/constructores en tiempo de compilación
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <groupId>com.tecnostock</groupId>
    <artifactId>tecnostock</artifactId>
    <version>1.0.0</version>
    <name>tecnostock</name>
    <description>Sistema de gestion de stock y compras a proveedores - TP Analisis y Diseno</description>

    <properties>
        <java.version>17</java.version>
        <!-- Se fija explicitamente una version reciente de Lombok: las versiones
             viejas (heredadas de spring-boot-starter-parent 3.3.4) rompen con
             compiladores javac muy nuevos (JDK 21+/24+/26) porque Lombok se
             engancha a clases internas del compilador que cambian entre versiones. -->
        <lombok.version>1.18.42</lombok.version>
    </properties>

    <dependencies>
        <!-- Capa WEB / Controladores -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Vista: motor de plantillas Thymeleaf -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- ORM: Spring Data JPA (Hibernate por debajo) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Driver de conexion a MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Seguridad: login usuario/contraseña -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Integracion Spring Security + Thymeleaf (etiquetas sec:* en las vistas) -->
        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
        </dependency>

        <!-- Validaciones sobre los DTO (Bean Validation / Jakarta Validation) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Lombok: reduce codigo repetitivo (getters, setters, constructores) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
